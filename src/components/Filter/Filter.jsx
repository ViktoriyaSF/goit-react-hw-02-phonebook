import propTypes from 'prop-types';
import React from 'react';
import { nanoid } from 'nanoid';

export const Filter = ({}) => {
  return (
    <>
      <label htmlForm={nanoid()}></label>
      <input></input>
    </>
  );
};
